# Running the Server

./server.py --start 
===================

./server.py --stop
==================

# Running the Client


./myfpt.py -r  HOST   -p  PORT
==============================

HOST defaultS to 127.0.0.1
PORT defaults to 42
